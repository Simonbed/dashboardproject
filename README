The apriori_dash.py requires the 2 SAS databases to work.


The dashboard_FRED.py doesn't require any database since it gets the data from fred st louis API.



These both make use of "DASH" to create an interractive dashboard.
